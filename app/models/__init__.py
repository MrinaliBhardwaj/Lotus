"""SQLAlchemy ORM models — implemented in Task 2 (schema + migrations).

This module must import every model so Alembic autogenerate sees the full
metadata. Keep it the single registration point.
"""
